const HeaderHeight = 50
const FooterHeight = 50
export { HeaderHeight, FooterHeight }

// TODO: These value SHOULD be in the mantine theme object since they are
// theme related and primarily used in mantine components with (theme) => {} syntax
